<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET used to be declared here and in the profile source set, which
         is what Flutter's template does for hot reload. The app needs it in
         every build type, so the main manifest declares it and this source set
         has nothing left to add. -->
</manifest>
